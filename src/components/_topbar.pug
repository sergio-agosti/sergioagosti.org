header(class="sticky top-0 border-b glow-border bg-gray-100/70 glow backdrop-blur dark:bg-gray-900/70 print:hidden")
  div(class="container flex items-center justify-between py-3")
    include @/components/_site-nav.pug
    unless current === "thank-you"
      button(class="button small max-sm:hidden", commandfor="contact-dialog", command="show-modal") Contact me
    include @/components/_theme-toggle.pug
